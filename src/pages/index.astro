---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

import '../styles/global.css';

const projects = await getCollection('projects');
console.log(projects);
---

<Layout 
	title="Cristian Cantero López | Desarrollador Web Full Stack en JavaScript, React y Node.js" description="Desarrollador web Full Stack especializado en JavaScript, React, Node.js y tecnologías modernas como Astro y Tailwind CSS. Ayudo a crear sitios web rápidos, escalables y con un diseño atractivo."
>
	<div class="text-center text-2xl font-bold mt-10 mb-10">Cristian Dev 💻</div>
	<section aria-label="Proyectos destacados de Cristian Cantero López" class="p-4 my-10 mx-auto max-w-6xl">
		<div class="grid grid-cols-2 md:grid-cols-1 gap-6">
			<h1 class="text-2xl text-center font-bold mb-4">Proyectos destacados</h1>
			{
				projects.map(project => {
					const { title, description, image, technologies, url } = project.data;
					return (
						<div class="bg-gray-800 p-4 rounded-lg shadow-md">
							<h2 class="text-2xl font-bold">{title}</h2>
							<p class="my-4 text-gray-400">{description}</p>
							<img src={image} alt={title} class="w-full h-auto mt-4 rounded-md" />
							<div class="my-6 mx-auto flex flex-wrap justify-center">
								{technologies.map(tech => (
									<span class="inline-block text-white px-3 py-1">
										<img src={tech.icon} alt={tech.name} class="inline-block w-14 h-14 mr-1" />
									</span>
								))}
							</div>
							<div class="mt-4">
								<a href={url.github} class="text-blue-500 hover:underline">Repositorio</a>
								{url.demo && (
									<a href={url.demo} class="text-blue-500 hover:underline ml-4">Demo</a>
								)}
							</div>
						</div>
					);
				})
			}
		</div>
	</section>
</Layout>
